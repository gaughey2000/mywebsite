# Copy this file to .env and fill in your actual values

# Where to send contact form messages
TO_EMAIL=your-email@protonmail.com

# Sender identity for outgoing emails
FROM_NAME=Your Name Portfolio
FROM_EMAIL=noreply@yourdomain.com

# === DEVELOPMENT (Mailtrap) ===
# Sign up at https://mailtrap.io (free)
# Go to Email Testing > Inboxes > Your Inbox > SMTP Settings
SMTP_HOST=sandbox.smtp.mailtrap.io
SMTP_PORT=2525
SMTP_USER=your-mailtrap-username
SMTP_PASS=your-mailtrap-password

# === PRODUCTION (ProtonMail Bridge) ===
# Download ProtonMail Bridge: https://proton.me/mail/bridge
# Start the bridge app and get local SMTP credentials
# SMTP_HOST=127.0.0.1
# SMTP_PORT=1025
# SMTP_USER=your-protonmail@protonmail.com
# SMTP_PASS=your-bridge-password

# Server Configuration
PORT=3001
CLIENT_ORIGIN=http://localhost:5173
NODE_ENV=development